plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "com.codestudio.editor"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.codestudio.editor"
        minSdk = 24
        targetSdk = 34
        versionCode = 2
        versionName = "1.1.0"
    }

    // Sign with local keystore if present (generate: see README.md)
    val keystoreFile = file("codestudio.keystore")
    if (keystoreFile.exists()) {
        signingConfigs {
            create("release") {
                storeFile = keystoreFile
                storePassword = "codestudio2024"
                keyAlias = "codestudio"
                keyPassword = "codestudio2024"
            }
        }
    }

    buildTypes {
        release {
            isMinifyEnabled = false
            if (keystoreFile.exists()) {
                signingConfig = signingConfigs.getByName("release")
            }
            proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }
    kotlinOptions {
        jvmTarget = "1.8"
    }
}

dependencies {
    // Pure Android SDK + Kotlin stdlib — no external dependencies,
    // everything (editor, preview, explorer) is self-contained for offline use.
}
